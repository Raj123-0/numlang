---
phase: 08-high-performance-numerical-benchmark-suite-victory-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/typecheck/checker.rs
  - src/codegen/cranelift_backend.rs
  - tests/benchmark_harness.rs
autonomous: true
requirements:
  - BENCH-02
  - BENCH-03
must_haves:
  truths:
    - "Benchmark harness implements 4 core comparative workloads: Recursive Fibonacci (fib 35), Math Loop Accumulator (10M iters), Hardware SIMD Vector Dot Product (10M iters), and Dense Matrix-Vector Multiplication (1M iters)"
    - "All workloads run across numlang, optimized Rust (rustc -O), and optimized C (cl.exe /O2) on Windows"
    - "All implementations produce bit-for-bit verified identical numerical outputs and exit codes"
    - "numlang demonstrates statistically significant performance advantages across workloads"
  artifacts:
    - src/typecheck/checker.rs
    - src/codegen/cranelift_backend.rs
    - tests/benchmark_harness.rs
  key_links:
    - "tests/benchmark_harness.rs executes the comparative benchmark suite and validates execution times and exit codes"
---

<objective>
Expand the comparative benchmark suite to 4 extensive numerical workloads (Fibonacci 35, 10M Math Accumulator, 10M SIMD Dot Product, and Matrix-Vector Multiplication) and verify numlang's performance advantages against Rust (`rustc -O`) and MSVC C (`cl.exe /O2`).

Purpose: Achieve and demonstrate verified Benchmark Supremacy across all standardized numerical computing tasks.
Output: Extended comparative benchmark harness in `tests/benchmark_harness.rs` with verification report.
</objective>

<execution_steps>
1. Add `to_i64` intrinsic to `src/typecheck/checker.rs` and `src/codegen/cranelift_backend.rs` for float-to-integer conversion (`fcvt_to_sint`).
2. Update `tests/benchmark_harness.rs` to implement the 4 comprehensive workloads:
   - Workload 1: Recursive Fibonacci (fib 35)
   - Workload 2: Tight Math Loop Accumulator (10M iters)
   - Workload 3: Hardware SIMD / FMA Vector Dot Product (10M iters)
   - Workload 4: Dense Matrix-Vector Multiplication (1M iters)
3. Implement equivalent Rust (`rustc -O`) and C (`cl.exe /O2`) versions with identical inputs and outputs.
4. Execute the comparative benchmark harness and measure min and avg execution times.
5. Confirm output equivalence and record speedup margins in `08-VERIFICATION.md`.
</execution_steps>
