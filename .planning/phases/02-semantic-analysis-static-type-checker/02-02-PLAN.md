---
phase: 02-semantic-analysis-static-type-checker
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/typecheck/checker.rs
  - src/typecheck/typed_ast.rs
  - src/typecheck/mod.rs
  - src/diagnostic.rs
  - src/main.rs
  - tests/typecheck_tests.rs
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
must_haves:
  truths:
    - "Type checker catches and rejects all mixed-type arithmetic without implicit conversion"
    - "Type checker rejects reassignments to immutable variables"
    - "Type checker enforces return types and function call signatures"
    - "Type checker produces TypedProgram ready for IR lowering"
    - "Diagnostics display rich terminal formatting with spans and help messages"
  artifacts:
    - src/typecheck/checker.rs
    - src/typecheck/typed_ast.rs
    - tests/typecheck_tests.rs
  key_links:
    - "src/typecheck/checker.rs validates AST against ScopeEnvironment and Type rules"
    - "src/typecheck/typed_ast.rs stores validated typed AST"
    - "src/diagnostic.rs maps TypeError to miette CompilerDiagnostic"
---

<objective>
Implement the static type checker, semantic validation passes, typed AST representation, diagnostic error reporting, and comprehensive test suite.

Purpose: Complete Phase 2 by validating all programs against strict type safety, immutability, and scoping requirements, generating typed AST for Phase 3.
Output: Full `TypeChecker` implementation, typed AST, rich compiler diagnostics for type errors, `--check` CLI flag, and unit/integration test suite.
</objective>

<execution_steps>
1. Create `src/typecheck/typed_ast.rs` defining `TypedProgram`, `TypedFunction`, `TypedBlock`, `TypedStmt`, `TypedExpr` (attaching `Type` to expressions).
2. Implement `TypeChecker` in `src/typecheck/checker.rs` and `TypeError` enum with full source span attribution.
3. Validate literals with default inference (`i64`, `f64`, `bool`) and annotated types.
4. Validate binary operations (strict matching numeric/bool types, no implicit casting), unary ops, if/while conditions (`bool`), function calls, and returns.
5. Update `src/diagnostic.rs` with `miette` conversions for all `TypeError` variants.
6. Update `src/main.rs` to support `--check` flag and run type checking before AST emission.
7. Write exhaustive unit/integration tests in `tests/typecheck_tests.rs`.
</execution_steps>
