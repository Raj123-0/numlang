---
phase: 02-semantic-analysis-static-type-checker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/token.rs
  - src/ast.rs
  - src/parser/stmt.rs
  - src/lib.rs
  - src/typecheck/mod.rs
  - src/typecheck/types.rs
  - src/typecheck/symtab.rs
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
must_haves:
  truths:
    - "numlang defines primitive types i32, i64, f32, f64, bool, void"
    - "SymbolTable manages scoped variable bindings with mutability tracking"
    - "Parser supports let mut bindings and assignment statements x = expr;"
    - "Variable lookup respects lexical scoping and nested block shadowing"
  artifacts:
    - src/typecheck/types.rs
    - src/typecheck/symtab.rs
    - src/typecheck/mod.rs
  key_links:
    - "src/typecheck/types.rs provides Type enum used across semantic analysis"
    - "src/typecheck/symtab.rs maintains Symbol and ScopeEnvironment"
---

<objective>
Implement the type representation and symbol table environment, and extend the parser with mutability and assignment support.

Purpose: Provide the foundational data structures needed by the type checker to resolve symbols, check types, and enforce variable scoping and immutability rules.
Output: `Type` definitions, `ScopeEnvironment` / `SymbolTable`, updated token and parser supporting `let mut` and assignment statements `x = expr;`.
</objective>

<execution_steps>
1. Update `src/token.rs` to include `Token::Mut` with keyword `"mut"`.
2. Update `src/ast.rs` to include `is_mutable: bool` on `Stmt::Let`, and add `Stmt::Assign { name: String, value: Expr, span: Span }`.
3. Update `src/parser/stmt.rs` to parse `let mut x: i64 = 5;` and assignment statements `x = 10;`.
4. Create `src/typecheck/types.rs` defining `Type` (`I32`, `I64`, `F32`, `F64`, `Bool`, `Void`), helper methods (`is_numeric`, `from_str`).
5. Create `src/typecheck/symtab.rs` implementing `Symbol`, `FunctionSig`, and `ScopeEnvironment` with nested scoping, shadowing, and error handling.
6. Expose `pub mod typecheck;` in `src/lib.rs`.
</execution_steps>
