---
phase: 01-lexer-parser-ast-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/span.rs
  - src/token.rs
  - src/ast.rs
  - tests/lexer_tests.rs
autonomous: true
requirements:
  - LEX-01
  - LEX-02
must_haves:
  truths:
    - "numlang tokenizer accurately identifies all numeric literals (integers, floats) and math operators"
    - "numlang tokenizer identifies all language keywords (fn, let, return, if, else, while, for)"
    - "Source byte spans are preserved for all emitted tokens"
  artifacts:
    - Cargo.toml
    - src/token.rs
    - src/ast.rs
    - src/span.rs
    - tests/lexer_tests.rs
  key_links:
    - "src/token.rs uses src/span.rs for source code tracking"
    - "src/ast.rs defines strongly typed structures consumed by parser"
---

<objective>
Bootstrap the numlang Cargo crate and implement the high-performance lexer and typed AST definitions.

Purpose: Provide the foundational token stream and AST definitions required for parsing mathematical expressions and program declarations.
Output: Cargo project configured with logos, complete Token enum, Span struct, AST definitions, and unit test suite.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lexer-parser-ast-diagnostics/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Cargo workspace and dependencies</name>
  <files>Cargo.toml, src/lib.rs</files>
  <read_first>.planning/phases/01-lexer-parser-ast-diagnostics/01-RESEARCH.md</read_first>
  <action>
    Create a Rust library and binary crate in Cargo.toml.
    Add dependencies:
    - `logos` = "0.14"
    - `clap` = { version = "4.5", features = ["derive"] }
    - `miette` = { version = "7.4", features = ["fancy"] }
    - `thiserror` = "2.0"
    Initialize src/lib.rs with module declarations: `pub mod span; pub mod token; pub mod ast; pub mod parser; pub mod diagnostic;`.
  </action>
  <verify>cargo check</verify>
  <acceptance_criteria>
    - Cargo.toml contains `logos = "0.14"`
    - `cargo check` succeeds without build errors
  </acceptance_criteria>
  <done>Cargo project is initialized with all required compiler dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Define Span and AST data structures</name>
  <files>src/span.rs, src/ast.rs</files>
  <read_first>src/lib.rs</read_first>
  <action>
    Implement `src/span.rs`:
    - `Span` struct containing `start: usize`, `end: usize`, and helper methods `merge(&self, other: &Span) -> Span`.
    Implement `src/ast.rs`:
    - `Literal`: `Int(i64)`, `Float(f64)`, `Bool(bool)`.
    - `BinaryOp`: `Add (+)`, `Sub (-)`, `Mul (*)`, `Div (/)`, `Mod (%)`, `Pow (^)`, `Eq (==)`, `Ne (!=)`, `Lt (<)`, `Le (<=)`, `Gt (>)`, `Ge (>=)`.
    - `UnaryOp`: `Neg (-)`, `Not (!)`.
    - `Expr`: `Literal(Literal, Span)`, `Ident(String, Span)`, `Binary { op: BinaryOp, left: Box<Expr>, right: Box<Expr>, span: Span }`, `Unary { op: UnaryOp, expr: Box<Expr>, span: Span }`, `Call { callee: String, args: Vec<Expr>, span: Span }`.
    - `Stmt`: `Let { name: String, ty: Option<String>, value: Expr, span: Span }`, `Return(Option<Expr>, Span)`, `Expr(Expr)`.
    - `Block`: `Vec<Stmt>, Span`.
    - `Function`: `name: String`, `params: Vec<(String, String)>`, `return_ty: Option<String>`, `body: Block`, `span: Span`.
    - `Program`: `items: Vec<Function>`.
  </action>
  <verify>cargo test --lib</verify>
  <acceptance_criteria>
    - src/span.rs exports `Span`
    - src/ast.rs exports `Expr`, `Stmt`, `BinaryOp`, and `Program`
  </acceptance_criteria>
  <done>AST definitions accurately reflect mathematical expressions and statement structures.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Logos Tokenizer and Lexer Tests</name>
  <files>src/token.rs, tests/lexer_tests.rs</files>
  <read_first>src/ast.rs, src/span.rs</read_first>
  <action>
    Implement `src/token.rs` with `logos::Logos`:
    - Define tokens for keywords: `fn`, `let`, `return`, `if`, `else`, `while`, `for`.
    - Define tokens for operators: `+`, `-`, `*`, `/`, `%`, `^`, `==`, `!=`, `<`, `<=`, `>`, `>=`, `=`.
    - Define delimiters: `(`, `)`, `{`, `}`, `[`, `]`, `,`, `:`, `;`, `->`.
    - Parse numeric literals with strict regex: floats (`[0-9]+\.[0-9]+`) and integers (`[0-9]+`).
    - Parse identifiers (`[a-zA-Z_][a-zA-Z0-9_]*`).
    - Provide `tokenize(input: &str) -> Result<Vec<(Token, Span)>, LexError>`.
    Implement unit tests in `tests/lexer_tests.rs`:
    - Test basic arithmetic tokenization (`1 + 2 * 3.5 ^ 4`).
    - Test keyword detection (`fn add(x: f64) -> f64 { return x; }`).
    - Test span offsets and whitespace skipping.
  </action>
  <verify>cargo test --test lexer_tests</verify>
  <acceptance_criteria>
    - `cargo test --test lexer_tests` passes 100%
    - Tokens accurately distinguish floats, ints, keywords, and operators
  </acceptance_criteria>
  <done>Lexer fulfills requirements LEX-01 and LEX-02 with complete test coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check` compiles with zero warnings
- [ ] `cargo test --test lexer_tests` passes all test cases
- [ ] Code formatted with `cargo fmt`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Lexer tokenizes full grammar of Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-lexer-parser-ast-diagnostics/01-01-SUMMARY.md`
</output>
