# Plan 19-01: Workload Calibration in 14-Benchmark Multi-Language Suite

## Objective
Calibrate problem sizes in 	ests/multi_language_benchmarks.rs for the 4 previously narrow workloads across all 5 languages (numlang, Rust, C, Node.js, Python):
1. **Takeuchi Function**: Scale from 	ak(18, 12, 6) to 	ak(27, 18, 9).
   - Result: 18. Expected exit code: 18.
2. **Prime Counting**: Scale from count_primes(50000) to count_primes(400000).
   - Result: 33860 primes. Expected exit code: 68 (33860 % 256).
3. **Mandelbrot Grid**: Scale from mandelbrot(200, 200, 100) to mandelbrot(500, 500, 100).
   - Result: 5271482. Expected exit code: 186 (5271482 % 256).
   - In Python, ensure truncating division int(a / b) is used to maintain exact bit-for-bit parity with C, Rust, and numlang integer division semantics.
4. **Dense Matrix-Vector Multiply**: Scale from matvec_bench(1000000) to matvec_bench(5000000).
   - Expected exit code: 93.

## Implementation Details
1. Edit 	ests/multi_language_benchmarks.rs:
   - Workload 4 (Matrix-Vector Multiply): change parameter from 1000000 to 5000000 across numlang, Rust, C, Node.js, Python. Set expected_exit to 93.
   - Workload 6 (Prime Counting): change parameter from 50000 to 400000 across numlang, Rust, C, Node.js, Python. Set expected_exit to 68.
   - Workload 8 (Takeuchi Recursion): change parameter from (18, 12, 6) to (27, 18, 9) across numlang, Rust, C, Node.js, Python. Set expected_exit to 18.
   - Workload 12 (Mandelbrot Grid): change parameter from (200, 200, 100) to (500, 500, 100) across numlang, Rust, C, Node.js, Python. Set expected_exit to 186.
2. Verify all exit codes across all 5 languages.
