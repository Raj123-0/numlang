---
phase: 01-lexer-parser-ast-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/parser/mod.rs
  - src/parser/expr.rs
  - src/parser/stmt.rs
  - src/lib.rs
  - tests/parser_tests.rs
autonomous: true
requirements:
  - LEX-03
  - LEX-04
must_haves:
  truths:
    - "Pratt parser correctly handles operator precedence (+, -, *, /, %, ^) and associativity"
    - "Right-associativity of exponentiation (2 ^ 3 ^ 2 == 2 ^ (3 ^ 2)) is correctly parsed"
    - "Function declarations, let bindings, if/else, and while statements are parsed into AST"
  artifacts:
    - src/parser/mod.rs
    - src/parser/expr.rs
    - src/parser/stmt.rs
    - tests/parser_tests.rs
  key_links:
    - "src/parser/expr.rs uses tokens from src/token.rs and constructs AST nodes in src/ast.rs"
    - "src/parser/stmt.rs integrates expression parsing for statements and blocks"
---

<objective>
Implement the Pratt expression parser and recursive descent parser for statements, functions, and program grammar.

Purpose: Translate token streams into structured Abstract Syntax Trees with correct mathematical operator precedence and statement semantics.
Output: Complete parser engine in `src/parser/` with comprehensive unit test suite in `tests/parser_tests.rs`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-lexer-parser-ast-diagnostics/01-RESEARCH.md
@.planning/phases/01-lexer-parser-ast-diagnostics/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pratt Expression Parser</name>
  <files>src/parser/expr.rs, src/parser/mod.rs</files>
  <read_first>src/ast.rs, src/token.rs</read_first>
  <action>
    Implement Pratt parser in `src/parser/expr.rs`:
    - Define binding powers:
      - Comparison (`==`, `!=`, `<`, `<=`, `>`, `>=`): `(1, 2)`
      - Addition/Subtraction (`+`, `-`): `(3, 4)`
      - Multiplication/Division/Modulo (`*`, `/`, `%`): `(5, 6)`
      - Exponentiation (`^`): `(8, 7)` (Right-associative: left power > right power)
      - Unary Prefix (`-`, `!`): prefix binding power 9
    - Implement prefix parsing for literals, grouped expressions `(expr)`, unary negation, and identifiers / function calls.
    - Implement infix loop consuming operators while `min_bp < left_bp`.
  </action>
  <verify>cargo check</verify>
  <acceptance_criteria>
    - Pratt parser correctly builds binary expression trees
    - Exponentiation builds right-leaning tree for repeated powers
  </acceptance_criteria>
  <done>Mathematical expressions are parsed with standard precedence rules.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Statement and Declaration Grammar</name>
  <files>src/parser/stmt.rs, src/parser/mod.rs</files>
  <read_first>src/parser/expr.rs, src/ast.rs</read_first>
  <action>
    Implement recursive descent parsing in `src/parser/stmt.rs`:
    - Parse `let [name] (: [type]) = [expr];`
    - Parse `return [expr];`
    - Parse `if [expr] { [body] } else { [body] }`
    - Parse `while [expr] { [body] }`
    - Parse function declaration: `fn [name]([params]) -> [ret_type] { [body] }`
    - Parse full program items into `Program`.
  </action>
  <verify>cargo test --lib</verify>
  <acceptance_criteria>
    - Statements and blocks parse into `Vec<Stmt>`
    - Function definitions parse parameters, return types, and body blocks
  </acceptance_criteria>
  <done>Statement and function parsing completed according to numlang grammar.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Parser Test Suite</name>
  <files>tests/parser_tests.rs</files>
  <read_first>src/parser/mod.rs</read_first>
  <action>
    Create integration tests in `tests/parser_tests.rs`:
    - Assert `1 + 2 * 3` parses as `Add(1, Mul(2, 3))`
    - Assert `(1 + 2) * 3` parses as `Mul(Add(1, 2), 3)`
    - Assert `2 ^ 3 ^ 2` parses as `Pow(2, Pow(3, 2))`
    - Assert `fn square(x: f64) -> f64 { return x * x; }` parses into Function item with correct parameter and return type
    - Assert `if x > 0 { return 1; } else { return 0; }` parses accurately
  </action>
  <verify>cargo test --test parser_tests</verify>
  <acceptance_criteria>
    - `cargo test --test parser_tests` passes 100%
  </acceptance_criteria>
  <done>Parser handles all mathematical precedence and statement constructs reliably.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --test parser_tests` passes with zero failures
- [ ] Parser produces no infinite loops or unhandled token panics on valid input
</verification>

<success_criteria>
- All tasks completed
- Pratt parser and statement parser verified
- Requirements LEX-03 and LEX-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-lexer-parser-ast-diagnostics/01-02-SUMMARY.md`
</output>
