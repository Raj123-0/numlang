---
phase: 01-lexer-parser-ast-diagnostics
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/diagnostic.rs
  - src/main.rs
  - tests/cli_tests.rs
autonomous: true
requirements:
  - CLI-03
must_haves:
  truths:
    - "numlang executable supports --emit-tokens to print token stream with byte spans"
    - "numlang executable supports --emit-ast to print indented abstract syntax tree"
    - "Syntax errors display source file snippet with highlighted error span and descriptive message"
  artifacts:
    - src/diagnostic.rs
    - src/main.rs
    - tests/cli_tests.rs
  key_links:
    - "src/main.rs connects clap CLI arguments to tokenizer, parser, and diagnostic renderer"
    - "src/diagnostic.rs translates parser/lexer errors into formatted miette diagnostics"
---

<objective>
Build the CLI driver and diagnostic error reporting system for inspecting compiler frontend stages.

Purpose: Allow users and developers to inspect tokens, debug AST generation, and receive clear error messages with source code locations.
Output: CLI driver binary (`numlang`) supporting `--emit-tokens`, `--emit-ast`, and rich diagnostic error rendering.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-lexer-parser-ast-diagnostics/01-RESEARCH.md
@.planning/phases/01-lexer-parser-ast-diagnostics/01-01-PLAN.md
@.planning/phases/01-lexer-parser-ast-diagnostics/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Rich Diagnostic Reporting with Miette</name>
  <files>src/diagnostic.rs, src/lib.rs</files>
  <read_first>src/span.rs, src/token.rs, src/parser/mod.rs</read_first>
  <action>
    Implement `src/diagnostic.rs`:
    - Define `CompilerError` enum with `miette::Diagnostic` and `thiserror::Error`:
      - `UnexpectedToken { found: String, expected: String, span: SourceSpan, src: NamedSource<String> }`
      - `InvalidLiteral { msg: String, span: SourceSpan, src: NamedSource<String> }`
      - `UnterminatedBlock { span: SourceSpan, src: NamedSource<String> }`
    - Provide `render_error(err: &CompilerError)` helper function printing formatted diagnostics.
  </action>
  <verify>cargo check</verify>
  <acceptance_criteria>
    - `CompilerError` implements `miette::Diagnostic`
    - Error messages include source line, column pointers, and help hints
  </acceptance_criteria>
  <done>Diagnostic engine formats compiler errors cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI Flags and Driver</name>
  <files>src/main.rs</files>
  <read_first>src/diagnostic.rs, src/lib.rs</read_first>
  <action>
    Implement `src/main.rs` using `clap::Parser`:
    - Flags:
      - `file: Option<PathBuf>` (input numlang source file)
      - `--emit-tokens`: Tokenizes input and pretty-prints token list with spans.
      - `--emit-ast`: Parses input and pretty-prints structured AST representation.
    - Behavior:
      - If `--emit-tokens` is provided, print tokens and exit 0.
      - If `--emit-ast` is provided, print parsed AST and exit 0.
      - If parsing or tokenizing fails, render diagnostic via `miette` and exit with non-zero status code.
  </action>
  <verify>cargo run -- --help</verify>
  <acceptance_criteria>
    - `cargo run -- --help` displays `--emit-tokens` and `--emit-ast`
    - Command exits cleanly with informative usage instructions
  </acceptance_criteria>
  <done>CLI driver handles file inputs and frontend inspection flags.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Tests for CLI and Diagnostics</name>
  <files>tests/cli_tests.rs</files>
  <read_first>src/main.rs</read_first>
  <action>
    Implement integration tests in `tests/cli_tests.rs`:
    - Test valid file with `--emit-tokens`: verify output contains Token identifiers and integer/float literals.
    - Test valid file with `--emit-ast`: verify output contains structured expressions (e.g. `Binary { op: Add, ... }`).
    - Test invalid syntax: verify non-zero exit code and diagnostic output containing error message.
  </action>
  <verify>cargo test --test cli_tests</verify>
  <acceptance_criteria>
    - `cargo test --test cli_tests` passes 100%
  </acceptance_criteria>
  <done>CLI and diagnostic reporting verified via integration test suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test` passes all tests (lexer, parser, cli)
- [ ] `numlang --help` functions as expected
- [ ] Requirements CLI-03, LEX-01, LEX-02, LEX-03, LEX-04 verified
</verification>

<success_criteria>
- All tasks completed
- CLI flags working
- Requirement CLI-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-lexer-parser-ast-diagnostics/01-03-SUMMARY.md`
</output>
