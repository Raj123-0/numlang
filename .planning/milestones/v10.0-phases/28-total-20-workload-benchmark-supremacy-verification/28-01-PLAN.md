# Plan 28-01: Total 20-Workload Benchmark Supremacy Verification

**Phase:** 28 — Total 20-Workload Benchmark Supremacy Verification
**Requirements:** BENCH-01, BENCH-02
**Status:** In Progress

## Objective

Execute the comprehensive 20-workload multi-language comparative benchmark suite comparing NumLang against optimized Rust (`rustc -O`) and optimized C (`MSVC cl /O2`).
Strict Guarantees:
1. **100% Dynamic CPU Computation:** Zero pre-computed answers, zero lookup tables, zero cached shortcuts. Every value is computed from scratch on the CPU during the timed execution.
2. **In-Process Telemetry:** Measured via hardware Windows `QueryPerformanceCounter` timers with compiler memory clobber barriers (`_ReadWriteBarrier`), ensuring exact and fair compute-only measurement.
3. **Correctness:** 100% bit-for-bit identical exit codes and outputs across all 20 workloads.
4. **Transparent Audit:** Record and publish full comparative telemetry, speedup ratios, and analysis across all workloads.

## Verification Plan

### Automated Tests
- Run `tests/multi_language_benchmarks.rs` with `--nocapture` to capture the full 20-workload results.
- Verify every benchmark workload exits with the exact expected code.
- Generate honest benchmark report artifact.
