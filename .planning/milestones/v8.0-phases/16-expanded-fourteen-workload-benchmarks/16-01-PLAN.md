# Plan 16-01: Expanded 14-Workload Multi-Language Benchmark Suite

## Goal
Expand `tests/multi_language_benchmarks.rs` from 10 to 14 canonical workloads across numlang, Rust (`rustc -O`), C (MSVC `cl /O2`), Node.js (V8), and Python 3.14:
1. Workload 11: N-Queens Backtracking Problem (`nqueens(12)`) -> expected exit: 120
2. Workload 12: Mandelbrot Complex Dynamics Grid (`mandelbrot(200, 200, 100)`) -> expected exit: 205
3. Workload 13: Modular Exponentiation Accumulator (`mod_pow(5000000)`) -> expected exit: 211
4. Workload 14: Monte Carlo Stochastic Geometry (`monte_carlo_pi(5000000)`) -> expected exit: 22

## Tasks
1. Implement all 4 workloads in `tests/multi_language_benchmarks.rs` in all 5 languages.
2. Verify exit code equivalence across all languages.
3. Keep Python iterations bounded (`py_iters = 1`) for heavy workloads to prevent timeout while capturing accurate timings.
4. Verify dual-metric telemetry (wall clock and kernel User CPU Time via `GetProcessTimes`).
