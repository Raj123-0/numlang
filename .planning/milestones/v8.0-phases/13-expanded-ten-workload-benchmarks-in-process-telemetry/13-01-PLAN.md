# Plan 13-01: Expanded 10-Workload Benchmark Suite & High-Resolution In-Process CPU Telemetry

## Goal
Expand `tests/multi_language_benchmarks.rs` to 10 comprehensive, world-class benchmark workloads across 5 languages (numlang, Rust, C, Node.js, Python), and integrate native Windows `GetProcessTimes` API to report both:
1. Process Wall-Clock Time (Min and Avg)
2. In-Process User Mode CPU Execution Time (Min and Avg)

## Workload Pantheon (10 Total):
1. **Recursive Fibonacci (`fib 35`)**: Classical linear recurrence
2. **Math Loop Accumulator (`10M iters`)**: Loop induction & modular arithmetic
3. **Hardware SIMD Vector Dot (`10M iters`)**: Hardware vectorization & dot products
4. **Matrix-Vector Multiplication (`1M iters`)**: Dense linear algebra
5. **Collatz Hailstone (`100k seeds`)**: Dynamic branching path length
6. **Prime Counting (`50k limit`)**: Nested quadratic trial division
7. **Horner's Polynomial Evaluation (`10M iters`)**: Polynomial FMA accumulation
8. **Takeuchi Function (`tak(18, 12, 6)`)**: Deep ternary tree recursion (63,609 calls)
9. **Numerical Quadrature Pi Riemann Sum (`50M intervals`)**: Heavy numerical integration & scaling
10. **Ackermann Function (`ack(3, 8)`)**: Deep nested hyper-exponential recurrence (2,642,860 calls)

## Tasks
1. Update `tests/multi_language_benchmarks.rs` with `GetProcessTimes` User CPU telemetry.
2. Implement workloads 8, 9, and 10 across all 5 languages (numlang, Rust, C, Node.js, Python).
3. Display comparative table with Wall-Clock times and User CPU times, proving the exponential advantage.
